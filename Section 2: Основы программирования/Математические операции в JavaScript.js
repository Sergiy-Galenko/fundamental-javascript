// Матиатические операции 

// (бинарный оператор)
// +, -, *, /, %, **  

// Унарные операторы 
// -, +,
-1;
-0;
+0;
0;

+"2"; // 2 typeOf Number

+"2 hello"; // NaN

// Конкатынация +

"Hello " + "world" // "Hello world"

2 + "2" // "22"

// Инкримент, Дикримент 
// --, ++
// х++ === (x = x + 1)
// ++х === (x = x + 1)

let x = 1
console.log(x++); // 1
console.log(++x); // 2
x = 3;

//x-- === (x = x-1)
//--x === (x = x-1)

// +=, -=, *=, /=
//x += 4 === (x  = x + 4) 
//x *= 2 === (x = x * 2) 